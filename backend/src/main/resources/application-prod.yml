# 放在 jar 同目录的 config\application-prod.yml（或直接 application.yml）
spring:
  datasource:
    url: jdbc:mysql://192.168.1.10:13306/coderzhang_blog?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: 你的密码
server:
  address: 0.0.0.0
  port: 9090
jwt:
  secret: 你的JWT密钥